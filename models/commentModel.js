export default class Comment {
  constructor(id, textContent, commentDate, user) {
    this.id = id;
    this.textContent = textContent;
    this.commentDate = commentDate;
    this.user = user;
  }
}